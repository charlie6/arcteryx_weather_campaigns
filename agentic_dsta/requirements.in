functions-framework==3.*
# The langchain, ag2 and llama_index extras were dropped. Nothing in this
# codebase imports LangChain, AutoGen or LlamaIndex -- they were boilerplate
# from the agent-starter-pack template -- and the langchain extra pins
# langchain-google-genai<4.2.3, every release of which requires
# google-genai<2.0.0. That directly contradicts google-adk>=2.9, which requires
# google-genai>=2.19, and makes the dependency set unresolvable.
#
# The >=1.160.0 floor matters too: the `adk` extra caps google-adk<2.0.0 up to
# and including 1.155.0, and only widens to <3.0.0 in 1.160.0.
google-cloud-aiplatform[agent_engines,adk]>=1.160.0,<2
a2a-sdk>=0.3.4
requests
google
google-ads
# google-adk >=2.0 is required for Gemini 3.x: it adds the `client` field on
# Gemini (replacing post-construction api_client assignment), thinking_config
# handling, and gemini-3 awareness. 1.17.0 has none of these.
google-adk>=2.9,<3
google-adk[eval]>=2.9,<3
# google-genai >=2.19 is required for the `us`/`eu` multi-region endpoints
# (_MULTI_REGIONAL_LOCATIONS -> https://aiplatform.us.rep.googleapis.com).
# 1.52.0 would build https://us-aiplatform.googleapis.com and fail.
google-genai>=2.19,<3
google-cloud-firestore
# Declared explicitly because this code imports them directly:
#   google.cloud.secretmanager -> tools/auth_utils.py
#   googleapiclient            -> tools/auth_utils.py, tools/sa360/*
# Until now they were only ever installed as transitive dependencies of
# google-adk 1.x. google-adk 2.x dropped both from its dependency set, which
# broke the container at import time. A direct import must always have a direct
# declaration; relying on another package's transitive graph is not a contract.
google-cloud-secret-manager
google-api-python-client
google-auth-oauthlib
python-dotenv
setuptools==80.9.0
pytest
coverage
pytest-mock